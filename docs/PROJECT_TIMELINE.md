# 轻设项目开发时间线

> 本文只记录已经发生的开发过程，不是后续路线图。
> 时间以中国标准时间（UTC+8）记录；没有可靠日历时间戳的内容不虚构具体日期。

## 时间总览

目前可以从仓库和构建记录核对到的开发窗口是：

- 起点：2026-07-10 17:00
- 当前可核对节点：2026-07-12 10:02
- 日历跨度：约 41 小时 2 分钟

产品定位、素材库规划和流程讨论早于代码记录，但当前项目文件没有可靠的日历时间戳，因此只按“代码记录之前的产品规划”保留，不填写虚构日期。

## 已发生的开发阶段

### 2026-07-10 17:00—17:59：编辑器工程基础

这一小时建立了第一轮可持续开发的编辑器基础：

- 建立安全验证边界
- 固定免费编辑器依赖
- 完成素材拖拽放置
- 支持无障碍拖拽
- 增加图层排序
- 增加图层显示、锁定和缩略图
- 使用 Dexie 进行本地存储和旧数据迁移
- 暴露本地保存状态
- 在图片导入修改数据前进行校验
- 修正响应式编辑器面板布局
- 对画布运行时进行懒加载

### 2026-07-11：编辑器交互优化与桌面化评估

这一阶段集中处理编辑器交互性能，并确定桌面 App 路线：

- 定位透视角度滑块卡顿根因
- 将实时透视预览改为 Fabric 原生变换
- 将精确透视栅格化限制在最终提交阶段
- 完成透视交互性能验证
- 评估并确定 Tauri Windows 桌面方案
- 确认继续复用 React + Fabric，不重写编辑器

### 2026-07-12：Windows App 原型与原生缓存

这一阶段完成了桌面 App 的核心存储接入：

- 接入 Tauri 桌面壳
- 项目数据使用原生 AppData 目录
- 云素材离线缓存使用原生 AppData 目录
- 修正 Tauri AppData 文件权限
- 浏览器版继续使用 IndexedDB 作为回退
- 完成类型检查、静态检查和 Web 生产构建
- 成功执行 Tauri Windows 构建
- 生成 NSIS Windows 安装包
- 成功启动生成的桌面版

安装包生成节点：2026-07-12 10:02 左右。

## 当前节点

当前项目处于：

> Windows 可运行 App 原型 + 云素材后台 + 本地离线缓存

核心链路已经形成：

```text
素材管理面板入库
        ↓
素材后台保存
        ↓
编辑器获取素材
        ↓
拖拽到现场照片
        ↓
调整并导出设计图
```

## 时间线使用规则

- 新增已完成阶段时，按实际完成日期追加到本文。
- 未来计划另建路线图，不混入本时间线。
- 如果只有对话记录、没有可靠日期，只记录阶段，不填写猜测日期。
